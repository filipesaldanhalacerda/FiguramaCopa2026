# Backend OPCIONAL (Supabase). O app funciona 100% local sem isto.
# Ao preencher, habilita auth real, sync, match com gente de verdade e chat realtime.
# Copie para ".env.local" e preencha com os valores do seu projeto Supabase.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# NUNCA coloque a service_role key aqui (é só pro servidor / Edge Functions).
